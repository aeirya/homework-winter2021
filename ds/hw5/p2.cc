#include <iostream>

using std::cin;
using std::cout;
using std::endl;

class directed_graph {
    private: 
    
};

int main()
{
    int n,  // towns
        m;  // important pairs

    cin >> n >> m;

    int a[m],  // from 
        b[m];  // to

    for (int i=0; i<m; ++i)
        cin >> a[i] >> b[i];
}