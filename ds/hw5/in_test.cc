#include <iostream>
using std::cin;
using std::cout;
using std::endl;

struct edge {
    int from;
    int to;
    int weight;
};

int main() {
    int x,y;
    cin >> x >> y;
    edge e;
    cin >> e.from >> e.to >> e.weight;
    cout << "Lol" << endl;
    return 0;
}