#include "iostream"
#include "list"
#include "array"

using std::array;

void median_of_five(array<int, 5> A, int start_index) {
    A
}

int find_median(int A[], int n) {
     auto x = std::list<int>;
    //  x.
}

int main() {
    int n, k;
    std::cin >> n >> k;

    std::array<int,n> A;
    for (int i=0; i<n; ++i)
        std::cin >> A[i];


    
}